# Use Cases

- Copilot CLI switch gate MCP: evaluate Copilot CLI switch gate MCP with public-safe inputs.
- Copilot CLI Switch Gate: evaluate Copilot CLI switch gate MCP with public-safe inputs.
- Copilot CLI Switch Gate documentation: evaluate Copilot CLI switch gate MCP with public-safe inputs.
- Copilot CLI Switch Gate remote MCP: evaluate Copilot CLI switch gate MCP with public-safe inputs.
- copilotcliswitchgate server card: evaluate Copilot CLI switch gate MCP with public-safe inputs.

## Good First Pilot

Pick one bounded team workflow, use non-sensitive examples, and verify the generated evidence with the person who owns the decision.
